# Niha Model Intelligence backend — Phase 0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2,<3
PyYAML>=6
numpy>=1.26

# Embedding Level-1 classifier (default; SEMANTIC=1). Set SEMANTIC=0 to run keyword-only
# without this dependency.
sentence-transformers>=2.2
